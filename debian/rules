#!/usr/bin/make -f
# managed by CDBS

include /usr/share/cdbs/1/rules/debhelper.mk

DEB_DH_INSTALL_ARGS=--exclude=debian/

SRC_VERSION := $(shell dpkg-parsechangelog | grep '^Version:' | cut -d' ' -f2 | cut -d- -f1 | cut -d\: -f2)
binary-install/quantispci-dkms::
	cp $(CURDIR)/debian/dkms.conf $(CURDIR)/debian/$(cdbs_curpkg)/usr/src/quantispci-$(SRC_VERSION)

